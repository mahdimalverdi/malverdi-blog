## فصل یازدهم: وقتی «روی سیستم من کار می‌کند» دیگر کافی نیست — Containers و Kubernetes

TODO:

- صحنه: برنامه باید روی لپ‌تاپ، محیط تست و تولید تقریباً یکسان اجرا شود.
- Container را ساده توضیح بده: بسته‌بندی برنامه همراه با وابستگی‌های اجرایی‌اش.
- بعد Kubernetes را وارد کن: وقتی تعداد کانتینرها، سرویس‌ها، نسخه‌ها و نیاز به مقیاس‌دهی زیاد می‌شود.
- بگو Docker زودتر از Kubernetes معنی پیدا می‌کند؛ Kubernetes را نباید برای پروژه‌ی کوچک زود آورد.
- نکته‌ی تحلیلی: کانتینرها تکرارپذیری اجرا می‌دهند، ارکستریشن مدیریت مقیاس و پایداری را کمک می‌کند.
