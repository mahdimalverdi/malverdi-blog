## فصل اول: وقتی API از یک مسیر ساده تبدیل به قرارداد می‌شود — API-first Approach

TODO:

- با این صحنه شروع کن: اول فقط یک فرانت‌اند داریم و چند endpoint ساده.
- بعد اتفاق می‌افتد: موبایل، پنل ادمین یا شریک تجاری هم باید از سیستم استفاده کنند.
- توضیح بده API-first یعنی API را از اول مثل قرارداد قابل فهم و نسبتاً پایدار ببینیم.
- بگو چرا لازم نیست از روز اول سند عظیم و فرایند سنگین بسازیم.
- نکته‌ی تحلیلی: API خوب جلوی تغییر را نمی‌گیرد؛ تغییر را قابل مدیریت می‌کند.
